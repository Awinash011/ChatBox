<div class="row">
  <div class="col-md-4">
     <%= image_tag "img_two.jpg", :size => "400x300" %>
  </div>

  <div class="col-md-offset-1 col-md-7">
    <ul id="conversations-list">
      <%# @conversations.each do |conversation| %>
        <%= render 'conversations/conversation', conversation: @conversation, user: current_user if @conversation %>
      <%# end %>
    </ul>
  </div>

<!--   <div class="col-md-3 text inline">
    <div class="panel panel-primary">
      <div class="panel-heading">
        <h3 class="panel-title">User list</h3>
      </div>
      <div class="panel-body">
        <ul>
          <%# @users.each do |user| %>
            <li>
              <%#= link_to user.user_name, conversations_path(user_id: user), remote: true, method: :post %>
              <%#= user.user_name %>
            </li>
          <%# end %>
        </ul>
      </div>
    </div>
  </div> -->
</div>
